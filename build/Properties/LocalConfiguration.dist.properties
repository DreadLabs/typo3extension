# String: Name of the database host
DB.host=${targets.${deployTo}.db.host}
# String: Name of the database
DB.database=${targets.${deployTo}.db.name}
# String: Name of the database user
DB.username=${targets.${deployTo}.db.username}
# String: Database password
DB.password=${targets.${deployTo}.db.password}
# String: Extension tables definition script
DB.extTablesDefinitionScript=extTables.php
